server:
  servlet:
    context-path: /photoAlbum

spring:
  datasource:
    username: root
    password: toor
    url: jdbc:mysql://localhost:3306/photodb
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none