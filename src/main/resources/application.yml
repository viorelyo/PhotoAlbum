server:
  servlet:
    context-path: /photoAlbum

spring:
  datasource:
    username: ${db.username}
    password: ${db.password}
    url: jdbc:mysql://localhost:3306/photoalbum
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none